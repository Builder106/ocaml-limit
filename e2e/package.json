{
  "name": "ocaml-lob-e2e",
  "private": true,
  "description": "Playwright + Gherkin demo-recording suite for the OCaml LOB dashboard. Produces mp4 demos that the README embeds.",
  "type": "module",
  "scripts": {
    "demo": "DEMO=1 bddgen --config playwright.demo.config.ts && DEMO=1 playwright test --config playwright.demo.config.ts",
    "demo:headed": "DEMO=1 bddgen --config playwright.demo.config.ts && DEMO=1 playwright test --config playwright.demo.config.ts --headed",
    "gifs": "bash scripts/to-gifs.sh",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.0.1",
    "playwright-bdd": "^9.2.0",
    "typescript": "^6.0.3"
  }
}
