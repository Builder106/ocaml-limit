(executable
 (name bench)
 (public_name ocaml_lob_bench)
 (libraries ocaml_lob unix)
 (ocamlopt_flags
  (:standard -O3 -unbox-closures)))

(executable
 (name server)
 (public_name ocaml_lob_server)
 (libraries ocaml_lob dream yojson unix)
 (preprocess (pps lwt_ppx))
 (ocamlopt_flags
  (:standard -O3 -unbox-closures)))

; Allocation diagnostic: measures bytes-per-order via Gc.minor_words.
; Run with `dune exec bin/diag.exe` to measure cold-path warmup vs.
; per-submit allocation across different order counts.
(executable
 (name diag)
 (libraries ocaml_lob unix)
 (ocamlopt_flags
  (:standard -O3 -unbox-closures)))
